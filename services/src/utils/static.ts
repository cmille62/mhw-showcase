export const Static = {
    SETTINGS_UPDATED_MESSAGE: "Settings updated successfully",

}

export const invalidParameters = (parameter: string) => {
    return `Request is missing parameter: ${parameter}`;
}

export default Static;
