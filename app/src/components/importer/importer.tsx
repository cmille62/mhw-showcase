import React, { FunctionComponent } from "react";
import { Pane, Table, Heading, Button } from "evergreen-ui";

interface Props {

}

export const Importer: FunctionComponent<Props> = (props: Props) => {
    return (
        <Pane>
            
        </Pane>
    );
}
